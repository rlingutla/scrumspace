import React from 'react';

export default class SprintRow extends React.Component {
  constructor(props){
		super(props);
	}

  //if clicked, it returns one thing, otherwise another
	render() {
    //if clicked
		return (); //this has textboxes
    //else
      //return(); //this doesn't, it is just a centered plus glyphicon
  }
}
